# REFRACT Configuration
# Copy this file to .env for local development
# At least one API key is required for critique, all three are recommended for best results

# Google Gemini API Key (Recommended - also used for image editing)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key (Optional - for GPT-4 Vision critique)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (Optional - for Claude Vision critique)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Gemini image editing configuration
# Model policy: pro (default) or flash
GEMINI_IMAGE_MODEL_POLICY=pro
# Override model explicitly if desired
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# Output size for Gemini 3 Pro Image (1K, 2K, 4K). Only used with pro.
# GEMINI_IMAGE_SIZE=2K
# Optional aspect ratio override (e.g., 1:1, 4:3, 16:9) or "auto"
# GEMINI_IMAGE_ASPECT_RATIO=auto
# Number of editing passes (1 = single pass)
# GEMINI_IMAGE_PASSES=1
